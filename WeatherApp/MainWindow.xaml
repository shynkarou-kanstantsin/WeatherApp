<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp"
        xmlns:userControls="clr-namespace:WeatherApp.View.UserControls"
        mc:Ignorable="d"
        Title="Weather"
        Icon="/Images/Icon.png"
        WindowStyle="None"
        ResizeMode="NoResize"
        Height="450" Width="800">
    
    <Border BorderBrush="#130b61" BorderThickness="1" CornerRadius="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="7*"></RowDefinition>
                <RowDefinition Height="93*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*"></ColumnDefinition>
                <ColumnDefinition Width="70*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Grid.ColumnSpan="2">
                <Rectangle Fill="#130b61" MouseLeftButtonDown="DragMoveWindow"/>

                <Label Content="Weather now!" Foreground="White" FontSize="15" Width="100" HorizontalAlignment="Left" Margin="15,0,0,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Background="Transparent" BorderBrush="Transparent" Width="40" Click="MinimizeWindow">
                        <Button.Content>
                            <TextBlock Text="_" FontSize="15" Foreground="White"/>
                        </Button.Content>
                    </Button>

                    <Button Background="Transparent" BorderBrush="Transparent" Width="40" Click="CloseWindow">
                        <Button.Content>
                            <TextBlock Text="X" FontSize="15" Foreground="White"/>
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Rectangle Fill="#b0dceb"/>
                    <TextBlock Text="Pinned cities:" FontSize="20" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Rectangle Fill="#b0dceb"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding PinnedCities}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <userControls:CityTab Height="110" Width="200" Margin="0,0,0,5"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
           
            </Grid>

            <Border Grid.Row="1" Grid.Column="1" Background="#130b61" Width="1" HorizontalAlignment="Left"/>

            <Grid Grid.Column="1" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                    <RowDefinition Height="75*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Label Content="City" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="20" Margin="0,0,0,5"/>
                </Grid>
                
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*"></ColumnDefinition>
                        <ColumnDefinition Width="25*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                                 FontSize="20" Foreground="#130b61" Height="40" Width="300" HorizontalAlignment="Right" HorizontalContentAlignment="Left" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                    </Grid>

                    <Grid Grid.Column="1">
                        <Button Command="{Binding SearchCityCommand}" 
                        Background="Transparent" BorderBrush="#130b61" ToolTip="Search citys' weather" Width="30" Height="30" HorizontalAlignment="Left">
                            <Button.Content>
                                <TextBlock Text="" FontSize="15" FontFamily="Segoe MDL2 Assets" Foreground="Black" VerticalAlignment="Center"/>
                            </Button.Content>
                        </Button>
                    </Grid>
                    
                </Grid>

                <Grid Grid.Row="2">
                    <ItemsControl ItemsSource="{Binding CurrentSearchResult}" VerticalAlignment="Top">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#130b61" BorderThickness="1" Height="120" Width="200" CornerRadius="5" Margin="0,15,0,0">
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding CityName}" FontSize="20" 
                                   HorizontalAlignment="Center"/>
                                        <TextBlock FontSize="20" HorizontalAlignment="Center">
                                            <Run Text="{Binding Temperature}" />
                                            <Run Text="°C" />
                                        </TextBlock>
                                        <TextBlock Text="{Binding Condition}" FontSize="20" 
                                   HorizontalAlignment="Center"/>

                                        <Button Content="+" BorderBrush="Transparent" Background="Transparent" Foreground="Black" FontSize="20" Width="40" Height="40" 
                                            HorizontalAlignment="Right" ToolTip="Add city to pinned"
                                            Command="{Binding DataContext.PinCityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>
